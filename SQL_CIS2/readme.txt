

În fișierul `capitol=1058&capitol_vers=2015.js` trebuie adăugată autosuma pentru rândul 51, pe toate coloanele, din rândurile dinamice.

Trebuie adăugată încă o funcție, exact după modelul autosumelor existente pentru rândurile 10, 20, 30 și 40.

`ID_MD` și ceilalți parametri necesari se găsesc în fișierul Excel.

Te rog să faci modificările în GitHub, într-o versiune nouă a fișierului, iar apoi le vom trage local.

Mulțumesc frumos.

https://github.com/androidacvtivity/3-EDU

In the file `capitol=1058&capitol_vers=2015.js`, an autosum needs to be added for row 51 across all columns, based on the dynamic rows.

Please add another function, following exactly the same pattern as the existing autosum functions for rows 10, 20, 30, and 40.

The `ID_MD` values and the other required parameters are available in the Excel file.

Please make the changes in GitHub by creating a new version of the file, and then we will pull the changes locally.

Thank you very much.
